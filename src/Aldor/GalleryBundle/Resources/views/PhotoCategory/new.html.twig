<h1>Post creation</h1>

<form action="{{ path('aldor_inftech_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('aldor_inftech') }}">
            Back to the list
        </a>
    </li>
</ul>
