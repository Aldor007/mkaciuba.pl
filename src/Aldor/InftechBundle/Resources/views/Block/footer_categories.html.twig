<div class="widget col-xs-6 col-sm-2">
<h4 class="widget-title">{{title}}</h4>

    <ul class="entries links links-2-cols">
    {% for category in categories%}
        <li>
        <a href="{{ path('blog_category', {'slug': category.slug }) }}" class="entry-title">
            {{ category.name }}</a>
        <li>
    {% endfor %}
    </ul>
</div>

